# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
jinja2==3.1.4
python-multipart==0.0.9

# HTTP client (async, for ComfyUI API)
httpx==0.27.0

# Data validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# CLI
typer==0.12.3
rich==13.7.1

# Image processing
Pillow>=10.4.0

# Cloud storage — Cloudflare R2 via S3-compatible API
boto3==1.34.95
botocore==1.34.95

# Cloud storage — Google Cloud Storage (optional, alternative to R2)
google-cloud-storage>=2.18.0

# Google GenAI — Gemini API for prompt refinement, QC, screenplay gen
google-genai>=1.0.0

# Environment variables
python-dotenv==1.0.1

# API security & rate limiting
slowapi==0.1.9
python-jose[cryptography]==3.3.0

# Async SQLite for metadata persistence
aiosqlite==0.20.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-mock==3.14.0
httpx==0.27.0   # also used by TestClient

# Type stubs (dev)
types-Pillow==10.2.0.20240520
